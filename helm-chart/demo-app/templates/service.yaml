apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-demo-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-demo-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
